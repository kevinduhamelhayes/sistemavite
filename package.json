{
  "name": "sistemavite",
  "version": "1.0.0",
  "description": "Sistema de gestión para kioscos y pequeños mercados",
  "scripts": {
    "dev:api": "cd api && npm run dev",
    "dev:front": "cd front && npm run dev",
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:front\"",
    "init-db": "cd api && node src/scripts/initDb.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}